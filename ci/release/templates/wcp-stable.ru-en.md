# FreeWine 11 WCP

## RU
- Релиз содержит `freewine11-arm64ec`.
- Пакет публикуется как **content package** для Ae.solator и загружается через `Contents`.
- Релиз выкладывается в отдельный runtime-репозиторий (`wcp-runtime-lanes`) под тегом `freewine11-arm64ec-latest`.
- В `.wcp` включён forensic manifest (`share/wcp-forensics/*`).

## EN
- This release contains `freewine11-arm64ec`.
- The package is published as a **content package** for Ae.solator and downloaded via `Contents`.
- The release is published to the dedicated runtime repository (`wcp-runtime-lanes`) under tag `freewine11-arm64ec-latest`.
- The `.wcp` includes a forensic manifest (`share/wcp-forensics/*`).

## Included assets
- `freewine11-arm64ec.wcp`
- `SHA256SUMS-freewine11-arm64ec.txt`
