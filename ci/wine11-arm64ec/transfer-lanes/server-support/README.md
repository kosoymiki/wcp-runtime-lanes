This support lane is reserved for server-side protocol declarations that are only added
when a concrete `server-runtime` slice requires them.

Do not pre-seed it. Keep it empty until a runtime slice actually needs a protocol or
declaration change.
