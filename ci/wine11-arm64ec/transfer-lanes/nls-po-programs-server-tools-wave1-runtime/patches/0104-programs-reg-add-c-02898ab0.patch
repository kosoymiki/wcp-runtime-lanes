diff --git a/programs/reg/add.c b/programs/reg/add.c
index 484ace32d30a8540336e01922a40f6ee503895d2..e0cd0373339af4e08b8dbac4774feda80ff2efe9 100644
--- a/programs/reg/add.c
+++ b/programs/reg/add.c
@@ -52,12 +52,10 @@ static BOOL get_regdata(const WCHAR *data, DWORD reg_type, WCHAR separator,
                         BYTE **data_bytes, DWORD *size_bytes)
 {
     static const WCHAR empty;
-    int base;
 
     *size_bytes = 0;
 
     if (!data) data = &empty;
-    base = data[0] && towlower(data[1]) == 'x' ? 16 : 10;
 
     switch (reg_type)
     {
@@ -76,7 +74,7 @@ static BOOL get_regdata(const WCHAR *data, DWORD reg_type, WCHAR separator,
         {
             LPWSTR rest;
             unsigned long val;
-            val = wcstoul(data, &rest, base);
+            val = wcstoul(data, &rest, (towlower(data[1]) == 'x') ? 16 : 10);
             if (*rest || data[0] == '-' || (val == ~0u && errno == ERANGE)) {
                 output_message(STRING_MISSING_NUMBER);
                 return FALSE;
@@ -91,7 +89,7 @@ static BOOL get_regdata(const WCHAR *data, DWORD reg_type, WCHAR separator,
             WCHAR *rest;
             UINT64 val;
 
-            val = _wcstoui64(data, &rest, base);
+            val = _wcstoui64(data, &rest, (towlower(data[1]) == 'x') ? 16 : 10);
             if (*rest || (val == ~0ull && errno == ERANGE))
             {
                 output_message(STRING_MISSING_NUMBER);
