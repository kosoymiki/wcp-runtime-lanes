diff --git a/dlls/msi/format.c b/dlls/msi/format.c
--- a/dlls/msi/format.c
+++ b/dlls/msi/format.c
@@ -739,13 +739,7 @@
 
     while (*data)
     {
-        if (*data == '\\' && *(data + 1) == '[')
-        {
-            data += 2;
-            continue;
-        }
-
-        if (*data == '[')
+        if (*data == '[' && *(data - 1) != '\\')
             count++;
         else if (*data == ']')
             count--;
