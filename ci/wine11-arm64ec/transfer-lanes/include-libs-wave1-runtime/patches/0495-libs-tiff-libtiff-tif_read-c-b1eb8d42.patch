diff --git a/libs/tiff/libtiff/tif_read.c b/libs/tiff/libtiff/tif_read.c
index a2bb3046237d3e50277608357dc7cbe5aa997a21..7efab59c6a3827ec71b32896a4d0561299e94aeb 100644
--- a/libs/tiff/libtiff/tif_read.c
+++ b/libs/tiff/libtiff/tif_read.c
@@ -351,14 +351,15 @@ static int TIFFSeek(TIFF *tif, uint32_t row, uint16_t sample)
          * chunk strip */
         whole_strip = 1;
     }
+#else
+    whole_strip = 1;
+#endif
 
     if (!whole_strip)
     {
         /* 16 is for YCbCr mode where we may need to read 16 */
         /* lines at a time to get a decompressed line, and 5000 */
         /* is some constant value, for example for JPEG tables */
-
-        /* coverity[dead_error_line:SUPPRESS] */
         if (tif->tif_scanlinesize < TIFF_TMSIZE_T_MAX / 16 &&
             tif->tif_scanlinesize * 16 < TIFF_TMSIZE_T_MAX - 5000)
         {
@@ -369,9 +370,6 @@ static int TIFFSeek(TIFF *tif, uint32_t row, uint16_t sample)
             read_ahead = tif->tif_scanlinesize;
         }
     }
-#else
-    whole_strip = 1;
-#endif
 
     /*
      * If we haven't loaded this strip, do so now, possibly
@@ -385,22 +383,18 @@ static int TIFFSeek(TIFF *tif, uint32_t row, uint16_t sample)
             if (!TIFFFillStrip(tif, strip))
                 return (0);
         }
-#if defined(CHUNKY_STRIP_READ_SUPPORT)
         else
         {
             if (!TIFFFillStripPartial(tif, strip, read_ahead, 1))
                 return 0;
         }
-#endif
     }
 
-#if defined(CHUNKY_STRIP_READ_SUPPORT)
     /*
     ** If we already have some data loaded, do we need to read some more?
     */
     else if (!whole_strip)
     {
-        /* coverity[dead_error_line:SUPPRESS] */
         if (((tif->tif_rawdata + tif->tif_rawdataloaded) - tif->tif_rawcp) <
                 read_ahead &&
             (uint64_t)tif->tif_rawdataoff + tif->tif_rawdataloaded <
@@ -410,7 +404,6 @@ static int TIFFSeek(TIFF *tif, uint32_t row, uint16_t sample)
                 return 0;
         }
     }
-#endif
 
     if (row < tif->tif_row)
     {
@@ -473,9 +466,7 @@ int TIFFReadScanline(TIFF *tif, void *buf, uint32_t row, uint16_t sample)
     }
     else
     {
-        /* See TIFFReadEncodedStrip comment regarding TIFFTAG_FAXFILLFUNC. */
-        if (buf)
-            memset(buf, 0, (size_t)tif->tif_scanlinesize);
+        memset(buf, 0, (size_t)tif->tif_scanlinesize);
     }
     return (e > 0 ? 1 : -1);
 }
@@ -563,10 +554,7 @@ tmsize_t TIFFReadEncodedStrip(TIFF *tif, uint32_t strip, void *buf,
         stripsize = size;
     if (!TIFFFillStrip(tif, strip))
     {
-        /* The output buf may be NULL, in particular if TIFFTAG_FAXFILLFUNC
-           is being used. Thus, memset must be conditional on buf not NULL. */
-        if (buf)
-            memset(buf, 0, (size_t)stripsize);
+        memset(buf, 0, (size_t)stripsize);
         return ((tmsize_t)(-1));
     }
     if ((*tif->tif_decodestrip)(tif, buf, stripsize, plane) <= 0)
@@ -988,9 +976,7 @@ tmsize_t TIFFReadEncodedTile(TIFF *tif, uint32_t tile, void *buf, tmsize_t size)
         size = tilesize;
     if (!TIFFFillTile(tif, tile))
     {
-        /* See TIFFReadEncodedStrip comment regarding TIFFTAG_FAXFILLFUNC. */
-        if (buf)
-            memset(buf, 0, (size_t)size);
+        memset(buf, 0, (size_t)size);
         return ((tmsize_t)(-1));
     }
     else if ((*tif->tif_decodetile)(tif, (uint8_t *)buf, size,
@@ -1583,9 +1569,7 @@ int TIFFReadFromUserBuffer(TIFF *tif, uint32_t strile, void *inbuf,
         if (!TIFFStartTile(tif, strile))
         {
             ret = 0;
-            /* See related TIFFReadEncodedStrip comment. */
-            if (outbuf)
-                memset(outbuf, 0, (size_t)outsize);
+            memset(outbuf, 0, (size_t)outsize);
         }
         else if (!(*tif->tif_decodetile)(
                      tif, (uint8_t *)outbuf, outsize,
@@ -1612,9 +1596,7 @@ int TIFFReadFromUserBuffer(TIFF *tif, uint32_t strile, void *inbuf,
             if (!TIFFStartStrip(tif, strile))
             {
                 ret = 0;
-                /* See related TIFFReadEncodedStrip comment. */
-                if (outbuf)
-                    memset(outbuf, 0, (size_t)outsize);
+                memset(outbuf, 0, (size_t)outsize);
             }
             else if (!(*tif->tif_decodestrip)(
                          tif, (uint8_t *)outbuf, outsize,
